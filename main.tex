\documentclass[UTF8]{ctexbook}
\usepackage{polyglossia}

% 特殊字符支持
\usepackage{tipa}

%布局
\usepackage{multicol}
\usepackage{multirow} 
\usepackage{mdframed}
\usepackage{longtable}
\usepackage{enumitem} %阿语字体有时缺itemize引导符的字形
\usepackage{wrapfig} % 环绕图片的正文内容
\usepackage{amssymb}

% 页眉页脚自定义
\usepackage{fancyhdr}
\usepackage{fontspec}

% 突出显示和绘图
\usepackage{color}
\usepackage{graphicx}
\usepackage{tikz}
    \usetikzlibrary{3d, arrows.meta} % 屈折变化太多了，不用3D真不行
\usepackage{cancel} % 删除线

 \usepackage{layout}
% \usepackage[a4paper]{geometry}

\usepackage{marginnote}

% 页眉页脚格式
    \pagestyle{fancy}
    \fancyhf{}
    \setlength{\headheight}{17pt}
    \renewcommand{\headrulewidth}{0pt} % 删除页眉下横线
    % 偶数页外侧页眉（左侧）显示章标题
    \fancyhead[LE]{\emph{\fontspec{FandolKai-Regular}\leftmark}}
    % 奇数页外侧页眉（右侧）显示二级标题
    \fancyhead[RO]{\emph{\fontspec{FandolKai-Regular}\rightmark}}
    % 偶数页外侧页脚（左侧）显示页码
    \fancyfoot[LE]{\fontspec{FandolSong-Regular}\thepage}
    % 奇数页外侧页脚（右侧）显示页码
    \fancyfoot[RO]{\fontspec{FandolSong-Regular}\thepage}

    \fancypagestyle{plain}{
        \fancyhf{} % 清空
        \fancyfoot[LE]{\fontspec{FandolSong-Regular}\thepage}
        \fancyfoot[RO]{\fontspec{FandolSong-Regular}\thepage}
    }
% FIXME: Arabic环境下（description环境下发现的，但似乎只要是在Arabic环境就会这样）跨页时，页码字体字号会变为阿语字体，所以这里强制指定一下。然而，这样的指定不会影响到汉字，具体问AI。
% 此外，该环境下跨页，会让节标题反向，如13.2显示为2.13。

\setotherlanguages{arabic}
\setotherlanguages{english}
\setotherlanguages{mongolian}

\newfontfamily\arabicfont[
    Script=Arabic, 
    BoldFont=KufiStandardGK, 
    ItalicFont=NotoNaskhArabic-Regular,
    % SlantedFont=BisbeeTrial-Thin,
    Scale=1.1]
{notosansrefill} % {MicrosoftSansSerif}
% 本项目的正文阿语字体是Noto Sans Arabic Regular，其中缺失的字符使用Noto Sans Regular替代。两个字库都是开放使用的，但出于某种技术原因，我这里不能公开更多细节，如果要自行编译，可以从网络获取Microsoft Sans Serif字体作为替代，并将这里的{notosansrefill}换成注释中的{MicrosoftSansSerif}。

\newfontfamily\mongolianfont[
    Script=Mongolian,
    ItalicFont=Fu_XM_YaBai_PSMT,
    Scale=1]
{NotoSansMongolian-Regular}

% 自己的感悟笔记
\newenvironment{note}{
    \itshape
    % \fontspec{FandolKai-Regular} % 换字体的话，一些转写字母会出问题，比如不会自动变化``''
    % \slshape
    \begin{mdframed}[linewidth=0pt, backgroundcolor=gray!15]
    \setlength{\parindent}{2em}
    \marginnote{\hspace{0.5cm}\Large \textcolor{blue!50!black}{\arm{\fontspec{Farah} مذكّرات}}\hspace*{0.5cm}}% 由于边栏的引入，直接插入图的note会有一个空行
    }{
    \end{mdframed}
}

% 课上的强调
\newenvironment{attention}{
    \itshape
    \marginnote{\hspace{0.5cm}\LARGE \textcolor{red!50!black}{\arm{\fontspec{DecoTypeThuluthII} نقاط هامّة}}\hspace*{0.5cm}}
    \begin{mdframed}[
        leftline=true,
        % leftline=false,
        rightline=false,
        topline=false,
        bottomline=false,
        linecolor=gray!60,
        linewidth=2pt
    ]
    \setlength{\parindent}{0pt}
    \everypar{\makebox[1.2em][l]{\textcolor{gray!50}{$\vartriangleright  $}}}
    \ignorespaces
    }{
    \end{mdframed}
}

% % 性数格式配合图标
% \newcommand{\yesge}{%
%     \tikz[baseline, inner sep=0pt]{% 
%         \fill[green!70!black, rounded corners=0.3em] 
%         (-0.6em, 0.3em - 0.6em) rectangle (0.6em, 0.3em + 0.6em);
%         \node[
%         text=white,               % 白色文字
%         inner sep=0pt,            % 无额外内边距
%         anchor=center             % 中心对齐
%         ] at (0,0.3em) {\it 格};    % 文字位于圆心
%     }
% }
% \newcommand{\noge}{%
%     \tikz[baseline, inner sep=0pt]{% 
%         \fill[
%             red, 
%             rounded corners=0.3em] 
%         (-0.6em, 0.3em - 0.6em) rectangle (0.6em, 0.3em + 0.6em);
%         \node[
%             text=white,               % 白色文字
%             inner sep=0pt,            % 无额外内边距
%             anchor=center             % 中心对齐
%         ] at (0,0.3em) {\it 格};  % 文字位于圆心
%         \draw[
%             black, 
%             line width=1pt] 
%         (-0.5em, 0.3em - 0.5em) -- ( 0.5em, 0.3em + 0.5em)
%         ( 0.5em, 0.3em - 0.5em) -- (-0.5em, 0.3em + 0.5em)
%         ; 
%     }
% }

% 双语支持
\newcommand{\arm}[1]{\textarabic{#1}} % 正文中夹杂阿语
\newcommand{\ait}[1]{\itshape #1}
\newcommand{\abf}[1]{\bfseries #1}
\newcommand{\crm}[1]{\textenglish{#1}} % 阿语环境中夹杂中文
\newcommand{\ac}[2]{#2\textcolor{gray!10}{\hrulefill}\mbox{\arm{#1}}} % 双语对照

% 辅助显示
% 表示变化方向的箭头，有两种方向
\newcommand{\cto}{$\rightarrow $ }
\newcommand{\ato}{$\leftarrow $ }
\newcommand{\tto}{%
    \iflanguage{arabic}{\ato}{\cto}%
}

\newcommand{\lecon}[1]{\fbox{P#1}} % 对应视频课程P数
\renewcommand{\ae}[1]{$\times $١٠\textsuperscript{#1}} % 大数章节用于缩写过多个0

\newcommand{\blue}[1]{\textcolor{blue}{#1}}
\newcommand{\red}[1]{\textcolor{red}{#1}}
\newcommand{\gray}[1]{\textcolor{gray}{#1}}

\title{
    \arm{\abf{\Huge اَللُّغَةُ الْعَرَبِيَّةُ}} \\ \textbf{阿拉伯语笔记\\
    \normalsize 基于{\tiny（已经跑路的）}万门大学《阿拉伯语初级（上）》课程整理}
    \vfill
}
\author{主讲教师：白\quad 野（\arm{مَنْصُورٌ}）}
\date{\today}
\begin{document}
\maketitle

\layout

\frontmatter
\input{preface.tex}

\tableofcontents

\mainmatter
\input{ch1.tex}
\setcounter{chapter}{7}
\input{lecon8.tex}
\input{lecon9.tex}
\input{lecon10.tex}
\input{lecon11.tex}
\input{lecon12.tex}
\input{lecon13.tex}
\input{lecon14.tex}
\input{lecon15.tex}

\input{lecon22.tex}

\backmatter
\input{annexe-duolingo.tex}

\end{document}
